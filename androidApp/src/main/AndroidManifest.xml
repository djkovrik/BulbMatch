<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission
        android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove" />
    <uses-permission
        android:name="com.google.android.finsky.permission.BIND_GET_INSTALL_REFERRER_SERVICE"
        tools:node="remove" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="BulbMatch"
        android:theme="@android:style/Theme.Material.NoActionBar">
        <!-- Reporting is opt-in at the host boundary and remains disabled when
             Firebase credentials are absent or the build is non-release. -->
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />
        <!-- Privacy flags are applied from common code before explicit init. -->
        <meta-data
            android:name="com.yandex.mobile.ads.AUTOMATIC_SDK_INITIALIZATION"
            android:value="false" />
        <!-- Provider-only debug/preinstall attribution surfaces are not part of
             BulbMatch's approved Yandex reporting contract. -->
        <provider
            android:name="com.yandex.mobile.ads.features.debugpanel.data.local.DebugPanelFileProvider"
            tools:node="remove" />
        <provider
            android:name="io.appmetrica.analytics.internal.PreloadInfoContentProvider"
            tools:node="remove" />
        <activity
            android:name="com.sedsoftware.bulbmatch.AppActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
